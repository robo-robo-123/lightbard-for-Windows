<UserControl x:Class="ModernUITwitterApp.Pages.BasicPage1"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mui="http://firstfloorsoftware.com/ModernUI"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Style="{StaticResource ContentRoot}">

    <Grid.RowDefinitions>
      <RowDefinition Height="30" />
      <RowDefinition Height="30" />
      <RowDefinition Height="*" />
      <RowDefinition Height="30" />
    </Grid.RowDefinitions>

    <StackPanel Orientation="Horizontal">
    <Button x:Name="openButton" Content="open" Click="openButton_Click"/>
    <Button x:Name="musicButton" Content="Music" Click="musicButton_Click" />
      <Button x:Name="tweetButton" Content="Tweet" Click="tweetButton_Click" />
    </StackPanel>
    <TextBox x:Name="tweetInputBox" Grid.Row="2" TextWrapping="Wrap" AcceptsReturn="True" />
    <TextBlock x:Name="musicBlock" Grid.Row="3" />
    <!--    <ScrollViewer>
            <StackPanel MinWidth="200">
                <TextBlock Text="BASIC PAGE" Style="{StaticResource Heading2}" />
                <TextBlock Text="Content goes here" />
            </StackPanel>
        </ScrollViewer>
      -->
    </Grid>
</UserControl>
